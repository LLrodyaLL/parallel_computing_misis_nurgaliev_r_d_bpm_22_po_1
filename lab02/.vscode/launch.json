{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run classic",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/matrix_opt_lab2.exe",
            "args": ["classic", "512"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": true,
            "MIMode": "gdb"
        }
    ]
}